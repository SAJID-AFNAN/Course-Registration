// import PropTypes from 'prop-types';

const Bookmark = () => {
    return (
        <div className="w-1/4">
            
        </div>
    );
};

Bookmark.propTypes = {
    
};

export default Bookmark;